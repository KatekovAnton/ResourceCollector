using System;
using Microsoft.Xna.Framework.Graphics;
using System.IO;

  /*namespace ResourceCollectorXNA.Content.Loaders
{
  class SkinnedMeshContent : PackContent
    {
        public override void Load(GraphicsDevice device, Stream stream, object data)
        {
            if (!(data is Skeleton))
                throw new Exception("Non skeleton for the 3rd parameter");

            Object = MemorySkinnedMesh.FromStream(device, stream, data as Skeleton);
        }
    }
}*/
